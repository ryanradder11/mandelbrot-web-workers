import { MillisecondsTransformPipe } from './milliseconds-transform.pipe';

describe('MillisecondsTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new MillisecondsTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
